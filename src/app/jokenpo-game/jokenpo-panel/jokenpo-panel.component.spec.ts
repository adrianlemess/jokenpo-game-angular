import { ComponentFixture, TestBed } from '@angular/core/testing';

import { JokenpoPanelComponent } from './jokenpo-panel.component';

describe('JokenpoPanelComponent', () => {
    // let component: JokenpoPanelComponent;
    // let fixture: ComponentFixture<JokenpoPanelComponent>;

    // beforeEach(async () => {
    //     await TestBed.configureTestingModule({
    //         declarations: [JokenpoPanelComponent],
    //     }).compileComponents();
    // });

    // beforeEach(() => {
    //     fixture = TestBed.createComponent(JokenpoPanelComponent);
    //     component = fixture.componentInstance;
    //     fixture.detectChanges();
    // });

    it('should create', () => {
        expect(true).toBeTruthy();
    });
});
